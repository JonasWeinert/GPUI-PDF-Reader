# Reference adversarial loop

This no-WASI Component Model security fixture activates normally and then loops
forever for every delivered event. The host must stop each call with its fuel
budget and suspend the package after repeated violations without blocking or
crashing the PDF reader.

It is test material, not an example extension to install for normal use.
