[package]
name = "key-editor-core"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Pure Markdown-backed rich-text editing model for Key applications"

[lib]
path = "src/lib.rs"

[dependencies]

[lints.rust]
unsafe_code = "forbid"
