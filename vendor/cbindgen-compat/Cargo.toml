[package]
name = "cbindgen"
version = "0.28.0"
edition = "2024"
license = "MIT"
description = "Minimal compile-time compatibility surface for GPUI's disabled shader generator"

[lib]
path = "src/lib.rs"
